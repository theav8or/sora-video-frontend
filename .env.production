# Backend API URL - will be /api in production (handled by Azure Static Web Apps routing)
VITE_API_URL=/api

# Frontend base URL (used for any absolute URLs in the app)
VITE_APP_URL=https://sora-wafl.azurewebsites.net

# Azure Static Web Apps will automatically handle routing /api/* to the backend
# No need to change the VITE_API_URL in production as it uses relative paths
